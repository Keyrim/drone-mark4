// The header compiled alone, under the flags of the target that owns it:
// it proves the header self-sufficient, and gives the language server the
// compile command it infers for a header from the source file of the same
// name next to it.
#include "ota/provider.hpp"
